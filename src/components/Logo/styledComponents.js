import styled from 'styled-components'
import colors from '../../constants/colors'

const LogoCosmetics = styled.div`
  font-size: 24px;
  font-weight: 700;
  line-height: 1.24;
  color: ${colors.logoOrange};
  cursor: pointer;

  span {
    color: ${colors.logoBlue};
  }
`
export default LogoCosmetics
